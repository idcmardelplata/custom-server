const ExpressInterface = require("./express_interface");
const HapiInterface = require("./hapi_interface");
const WebServer = require("./web_interface")

module.exports = {
  ExpressInterface,
  HapiInterface,
  WebServer
};
